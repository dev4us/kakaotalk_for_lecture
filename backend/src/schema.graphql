type Message {
  id: Int!
  text: String
  sendTo: User
  sendBy: User
  createdAt: String!
}

type Query {
  message: Message
  user: User
}

type User {
  id: Int!
  email: String!
  password: String!
  username: String!
  profilePhoto: String
  friends: [User]
}

type SignUpResponse {
  ok: Boolean!
  error: String
  token: String
}

type Mutation {
  SignUp(email: String!, password: String!, username: String!): SignUpResponse!
}
